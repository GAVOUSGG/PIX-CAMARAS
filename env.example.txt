# Variables de entorno para el proyecto
# Copia este archivo y renómbralo a .env en cada entorno

# ========== BACKEND (Railway) ==========
# Puerto del servidor (Railway lo asigna automáticamente)
PORT=3001

# URL del frontend (para CORS)
FRONTEND_URL=https://tu-proyecto.vercel.app

# Secret key para JWT (¡CAMBIA ESTO EN PRODUCCIÓN!)
JWT_SECRET=tu-secret-key-super-segura-aqui

# Base de datos PostgreSQL (Railway proporciona DATABASE_URL automáticamente)
# Si no está configurada, usará SQLite local
DATABASE_URL=postgresql://usuario:password@host:puerto/database

# Entorno
NODE_ENV=production

# ========== FRONTEND (Vercel) ==========
# URL del backend API
VITE_API_URL=https://tu-backend.railway.app

